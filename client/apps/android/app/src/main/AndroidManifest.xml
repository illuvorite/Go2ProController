<?xml version="1.0" encoding="utf-8"?>
<!-- Replace com.test.game with the identifier of your game below, e.g.
     com.gamemaker.game
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:versionCode="1"
    android:versionName="1.0"
    android:installLocation="auto">


    <!-- 局域网控制：网络与 WiFi 状态 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <!-- 组播发现（SN 多播 231.1.1.1:10131）必需，否则收不到任何回包 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>
    <!-- 注：曾试过加 MANAGE_EXTERNAL_STORAGE 去读官方宇树 App 的外部存储
         （/sdcard/Android/data/com.unitree.doggo2/），实测 Android 11+ 的 FUSE 会把
         其他 App 的 Android/data 整个隐藏（opendir 返回 ENOENT，与授权无关）→ 该权限无用，不加。 -->

    <!-- 部分机型扫描 WiFi 需要定位权限（仅用于读取网络信息，不采集位置） -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <!-- 触摸屏/GL 等 uses-feature 由 SDL 模板自带，勿重复声明（重复会 manifest 合并失败） -->
    <!-- OpenGL ES 2.0 -->
    <uses-feature android:glEsVersion="0x00020000" />

    <!-- Touchscreen support -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <!-- Game controller support -->
    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.gamepad"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false" />

    <!-- External mouse input events -->
    <uses-feature
        android:name="android.hardware.type.pc"
        android:required="false" />

    <!-- Audio recording support -->
    <!-- if you want to capture audio, uncomment this. -->
    <!-- <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" /> -->

    <!-- Allow downloading to the external storage on Android 5.1 and older -->
    <!-- <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="22" /> -->

    <!-- Allow access to Bluetooth devices -->
    <!-- Currently this is just for Steam Controller support and requires setting SDL_HINT_JOYSTICK_HIDAPI_STEAM -->
    <!-- <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" /> -->
    <!-- <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" /> -->

    <!-- Allow access to the vibrator -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- if you want to capture audio, uncomment this. -->
    <!-- <uses-permission android:name="android.permission.RECORD_AUDIO" /> -->

    <!-- Create a Java class extending SDLActivity and place it in a
         directory under app/src/main/java matching the package, e.g. app/src/main/java/com/gamemaker/game/MyGame.java
 
         then replace "SDLActivity" with the name of your class (e.g. "MyGame")
         in the XML below.

         An example Java class can be found in README-android.md
    -->
    <application android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true" >

        <!-- Example of setting SDL hints from AndroidManifest.xml:
        <meta-data android:name="SDL_ENV.SDL_ACCELEROMETER_AS_JOYSTICK" android:value="0"/>
         -->
     
        <!-- ★ 必须启动 MainActivity（不是 SDL 模板的 SDLActivity）：
             MainActivity 里才有 acquireMulticastLock()/releaseMulticastLock()，
             原生侧要用它申请 WifiManager.MulticastLock（否则 SN 组播发现收不到回包）。
             用 SDLActivity 时 JNI 反射找不到方法 → pending 异常 → 进程直接 abort。 -->
        <!-- ★ 方向：fullSensor = 四个方向都允许。
             改造前锁 sensorLandscape（只允许横屏）—— 那是因为界面按固定像素排版，
             手机竖屏下左栏（452dp）比屏宽还宽，直接溢出。现在有了断点布局
             （见 ui/layout.cpp：竖屏走「单栏 + 底部页签 + 悬浮双摇杆」），竖屏可以放开。
             configChanges 里已声明 orientation|screenSize，所以旋转不会重建 Activity，
             布局由 drawUi 每帧按新的 DisplaySize 重算。 -->
        <activity android:screenOrientation="fullSensor" android:name="org.libsdl.app.MainActivity"
            android:label="@string/app_name"
            android:alwaysRetainTaskState="true"
            android:launchMode="singleInstance"
            android:configChanges="layoutDirection|locale|orientation|uiMode|screenLayout|screenSize|smallestScreenSize|keyboard|keyboardHidden|navigation"
            android:preferMinimalPostProcessing="true"
            android:exported="true"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Let Android know that we can handle some USB devices and should receive this event -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <!-- Drop file event -->
            <!--
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            -->
        </activity>
    </application>

</manifest>
